import { Routes } from '@angular/router';
import { PizzaOrderingComponent } from './OrderManagement/pizza-ordering/pizza-ordering.component';
export const routes: Routes = [
    { path: '', component: PizzaOrderingComponent },
];
